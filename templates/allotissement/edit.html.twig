{% extends 'base.html.twig' %}

{% block title %}Modifier|Allotissement{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="offset-3 col-md-6">
            <h1>Modifier l'allotissement</h1>
            {{ form_start(form) }}
                {{ form_rest(form) }}
                {{ form_errors(form) }}
                <button class="btn">Modifier</button>
            {{ form_end(form) }}

        </div>
    </div>
</div>
{% endblock %}
